<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="WhiteFretboard" parent="Widget.AppCompat.Button">
        <item name="android:background" >@drawable/br_white_fretboard</item>
        <item name="android:layout_width">80dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_marginRight">2dp</item>
    </style>

    <style name="BlackFretboard" parent="Widget.AppCompat.Button">
        <item name="android:background" >@drawable/br_black_fretboard</item>
        <item name="android:layout_width">60dp</item>
        <item name="android:layout_height">90dp</item>
    </style>

</resources>